# Tab-to-Comma-delimiter
Removes tabs from a text file and replaces it with commas to import into an excel spreadsheet 


rename the file you want to fix to "data.txt"
new data should appear in "newdatafile.txt"
